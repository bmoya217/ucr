AVL Binary Search Tree
    the purpose is to use a tree to sort a list
        this will execute in O(n*ln(n)) rather than sorting in O(n^2)
    
    enter the name of the input file you wish to use as a command line argument
    the format should look like:
    
i
4 32 2 5 5 -3 0 -52
f
1 2 3 4 2 1 -4 -3 -3 1
.
.
.
    where i denotes integer and f denotes fraction
    th